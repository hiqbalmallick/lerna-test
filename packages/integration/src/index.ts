import { Doctor } from "@test-sdk/doctor";
import { Patient } from "@test-sdk/patient";
import { Scheduler } from "@test-sdk/scheduler";

console.log(new Doctor());
console.log(new Scheduler());
console.log(new Patient());
