{
  "packages": ["packages/*"],
  "version": "1.0.0",
  "npmClient": "npm",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.test.js", "docs/**/*.md"],
      "message": "chore(release): publish %s"
    }
  }
}
