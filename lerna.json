{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "npm",
  "useWorkspaces": false,
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.test.js", "docs/**/*.md"],
      "message": "chore(release): publish %s"
    }
  }
}
