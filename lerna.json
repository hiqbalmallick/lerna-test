{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "npm",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.test.js", "docs/**/*.md"],
      "message": "chore(release): publish %s"
    }
  }
}
