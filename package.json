{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "npx lerna publish",
    "prepublish": "npx lerna run tsc"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "lerna": "^3.22.1",
    "replace-in-file": "^7.1.0",
    "typescript": "^5.3.3"
  }
}
