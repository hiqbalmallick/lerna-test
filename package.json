{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "version": "npx lerna version",
    "build": "lerna run tsc",
    "publish": "npx lerna publish"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "lerna": "^3.22.1",
    "replace-in-file": "^7.1.0",
    "typescript": "^5.3.3"
  }
}
